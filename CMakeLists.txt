# the minimum required version is the same as in BirthView
cmake_minimum_required(VERSION 2.8.4)

# include the macros used in the subdirectories
include(CMake.Macros.cmake)

PROJECT(birthviewh)

add_subdirectory(bvh)
